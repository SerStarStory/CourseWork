@startuml
start
:Отримати потік лексем;
:Алгоритм Кока-Янгера-Касамі;
if (Успішно?) then (Так)
    :Побудува Абстрактного синтаксичного дерева;
    :Вивід АСД;
    :Збереження у файл;
    
else (Ні)
    end
endif
stop
@enduml
