@startuml
start
:Отримати потік лексем;
:Зібрати таблицю оголошених змінних tableA;
:Зібрати таблицю змінних, що використовуються tableB;
:Зібрати таблицю оголошених міток(labels) tableC;
:Зібрати таблицю міток(labels), що використовуються tableD;
:Отримати назву програми programName;
if (Є колізії між tableA і tableC і programName?) then (Так)
    :Помилка;
(Ні) elseif (Чи використовується неоголошена змінна?) then (Так)
    :Помилка;
(Ні) elseif (Чи використовується неоголошена мітка?) then (Так)
    :Помилка;
else (Ні)
    :Успіх;
endif
:Вивести результат в консоль;
:Вивести результат в файл;
if (Є помилки?) then (Так)
    end
else (Ні)
    stop
endif
@enduml